# Milestone 1 — ingestion
requests>=2.31
pypdf>=4.0
pdfplumber>=0.11
python-dotenv>=1.0
pytest>=8.0

# Milestone 2 — chunking + vector store
sentence-transformers>=3.0
chromadb>=0.5

# Milestone 2 — OCR fallback for the one confirmed-scanned document
# (minimum wage gazette notification). Also requires the Tesseract OCR
# engine installed separately (not pip-installable) — see README.md.
pymupdf>=1.24
pytesseract>=0.3

# Milestone 3 — API + generation
fastapi>=0.115
uvicorn[standard]>=0.32
# Groq free tier (openai/gpt-oss-120b / llama-3.1-8b-instant) — genuinely
# free, no billing account required (unlike Anthropic and Gemini, both of
# which gate their free tiers behind a card on file). See PROGRESS.md for
# the full provider story.
groq>=0.13

# Milestone 4 — eval
# RAGAS itself (ragas==0.4.3, the only version on PyPI) could not be made to
# import in this environment: it unconditionally imports an unused Google
# Vertex AI integration that no longer exists in current langchain-community,
# and installing it corrupted the project's numpy install (no prebuilt numpy
# wheel exists for Python 3.13, so pip fell back to a from-source MinGW
# build that segfaulted on `import numpy`). See PROGRESS.md for the full
# story. Per explicit user decision, eval/run.py implements the same four
# RAGAS metrics directly as LLM-judge prompts against the Groq client
# already listed above — no new dependency needed.

# Milestone 5 — hybrid search
rank_bm25>=0.2
